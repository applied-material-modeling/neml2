{
 "cells": [
  {
   "cell_type": "markdown",
   "id": "9c4793d6",
   "metadata": {},
   "source": [
    "(tensor-types-primitive)=\n",
    "# Primitive (fixed-base-shape) tensor types\n",
    "\n",
    "These are the wrappers you construct directly: a `Scalar`, a 3-vector,\n",
    "a symmetric second-order tensor, and so on. Each one fixes the shape\n",
    "*and* the packing convention of its trailing (base) axes — that is what\n",
    "separates an `SR2` from a bare `torch.Tensor` of shape `(6,)`."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "b285951c",
   "metadata": {
    "tags": [
     "remove-cell"
    ]
   },
   "outputs": [],
   "source": [
    "# Install NEML2 when running in Google Colab; no-op in the docs build and\n",
    "# local Jupyter, and hidden from the rendered page.\n",
    "import sys\n",
    "\n",
    "if \"google.colab\" in sys.modules:\n",
    "    !pip install -q neml2"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "b8999f68",
   "metadata": {},
   "source": [
    "## The class hierarchy\n",
    "\n",
    "Every primitive shares one base shape per type and inherits its\n",
    "arithmetic and factories from a common intermediate class:\n",
    "\n",
    "```\n",
    "TensorWrapper            (abstract — shape decomposition + region views)\n",
    "    └── PrimitiveTensor  (concrete intermediate — generic ops + factories)\n",
    "            ├── Scalar\n",
    "            └── Vec, R2, SR2, WR2, MRP, Quaternion, MillerIndex, SSR4\n",
    "```\n",
    "\n",
    "`PrimitiveTensor` is where the generic operators (`+`, `-`, `*`, `/`,\n",
    "`-x`) and the shape factories (`zeros`, `ones`, `full`, `empty`,\n",
    "`fill`) live. Each concrete leaf adds only its type-specific factories —\n",
    "e.g. `R2.identity`, `SSR4.identity_sym`, or `SR2.fill`'s Mandel-aware\n",
    "overloads."
   ]
  },
  {
   "cell_type": "markdown",
   "id": "7c9ed625",
   "metadata": {},
   "source": [
    "## The catalog\n",
    "\n",
    "| Type          | Base shape | Storage / convention |\n",
    "| :------------ | :--------- | :------------------- |\n",
    "| `Scalar`      | `()`       | A single number per batch entry. The wrapper exists so mixed ops like `Scalar * SR2` reliably return an `SR2`. |\n",
    "| `Vec`         | `(3,)`     | 3-vector. |\n",
    "| `MRP`         | `(3,)`     | Modified Rodrigues parameters for a 3D rotation: `n · tan(θ/4)`, zero vector = identity. |\n",
    "| `MillerIndex` | `(3,)`     | Integer crystallographic direction / plane normal, stored as float for differentiability. |\n",
    "| `Quaternion`  | `(4,)`     | Unit quaternion orientation, packed `(w, x, y, z)`. |\n",
    "| `R2`          | `(3, 3)`   | Full second-order tensor (no symmetry). |\n",
    "| `WR2`         | `(3,)`     | Skew-symmetric second-order tensor stored as an axial 3-vector `(w0, w1, w2)`; the 3×3 skew form is recovered by `r2_from_wr2`. |\n",
    "| `SR2`         | `(6,)`     | Symmetric second-order tensor in **Mandel notation**: `(σ₁₁, σ₂₂, σ₃₃, √2·σ₂₃, √2·σ₁₃, √2·σ₁₂)`. The off-diagonal `√2` makes the inner product `A : B = a · b` in 6-vector form. |\n",
    "| `SSR4`        | `(6, 6)`   | Fourth-order tensor with both minor symmetries (the elasticity-tensor symmetry class) in Mandel packing. |"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cd621fd3",
   "metadata": {},
   "outputs": [],
   "source": [
    "import torch\n",
    "from neml2.types import Scalar, Vec, MRP, MillerIndex, Quaternion, R2, WR2, SR2, SSR4\n",
    "\n",
    "for cls in (Scalar, Vec, MRP, MillerIndex, Quaternion, R2, WR2, SR2, SSR4):\n",
    "    print(f\"{cls.__name__:12s} BASE_SHAPE = {cls.BASE_SHAPE}\")"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "603d88d2",
   "metadata": {},
   "source": [
    "## Packing conventions are part of the type\n",
    "\n",
    "Because the convention lives in the type, the constructor does the\n",
    "packing for you. `SR2.fill` accepts the Mandel √2 scaling internally, so\n",
    "you pass physical components and get correct storage — the 1-, 3-, and\n",
    "6-component overloads cover a hydrostatic state, a diagonal state, and a\n",
    "general symmetric state respectively:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "ca2c1ae9",
   "metadata": {},
   "outputs": [],
   "source": [
    "print(\"SR2.fill(p)        ->\", tuple(SR2.fill(2.0).data.shape))\n",
    "print(\"SR2.fill(d1,d2,d3) ->\", tuple(SR2.fill(1.0, 2.0, 3.0).data.shape))\n",
    "print(\"SR2.fill(6 comps)  ->\", tuple(SR2.fill(1, 2, 3, 4, 5, 6).data.shape))\n",
    "print(\"SR2.identity()     ->\", SR2.identity().data)"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "b9737fbe",
   "metadata": {},
   "source": [
    "## dtype\n",
    "\n",
    "`Scalar` defaults to `torch.float64` (the precision NEML2's typed\n",
    "algebra runs in) for both literal construction and its factories. The\n",
    "other wrappers take an optional `dtype=` and otherwise fall through to\n",
    "torch's global default:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "f70acf8b",
   "metadata": {},
   "outputs": [],
   "source": [
    "print(\"Scalar(200e3).dtype  ->\", Scalar(200e3).dtype)\n",
    "print(\"Vec.zeros(5).dtype   ->\", Vec.zeros(5).dtype)\n",
    "print(\"Vec.zeros(5, dtype=float64) ->\", Vec.zeros(5, dtype=torch.float64).dtype)"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "f7e439c3",
   "metadata": {},
   "source": [
    "## Constructing wrappers\n",
    "\n",
    "Every primitive inherits the same shape-factory family from\n",
    "`PrimitiveTensor`. Each takes the **dynamic batch shape** as leading\n",
    "positional args and pads the call to torch with the type's base shape:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "5a62b443",
   "metadata": {},
   "outputs": [],
   "source": [
    "print(\"Vec.zeros(4)            ->\", tuple(Vec.zeros(4).data.shape))\n",
    "print(\"SR2.full(2, 3, fill=0.1)->\", tuple(SR2.full(2, 3, fill_value=0.1).data.shape))\n",
    "print(\"R2.identity()          ->\", tuple(R2.identity().data.shape))\n",
    "print(\"SSR4.identity_sym()    ->\", tuple(SSR4.identity_sym().data.shape))"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "603c77e8",
   "metadata": {},
   "source": [
    "`Scalar` adds the torch-analogue creation helpers, keeping the\n",
    "`float64` default:\n",
    "\n",
    "- `Scalar(<float>)` / `Scalar(<list>)` — direct literal coercion.\n",
    "- `Scalar.zeros` / `Scalar.ones` / `Scalar.full` — `float64` shape\n",
    "  factories.\n",
    "- `Scalar.arange(start, end, step)` — mirrors `torch.arange`.\n",
    "- `Scalar.from_value(x, like=other)` — promote a Python number,\n",
    "  inheriting `dtype` / `device` from an existing wrapper (handy inside a\n",
    "  leaf's `forward` to build an in-place neutral).\n",
    "\n",
    "To build a *ramp* between two endpoints, use the\n",
    "[](tensor-types-region-views) `linspace` / `logspace` free functions\n",
    "rather than a `Scalar` method — the target region is chosen by which\n",
    "region view you pass the endpoints as."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "2e8eb0a1",
   "metadata": {},
   "outputs": [],
   "source": [
    "print(\"Scalar([1, 2, 3]) ->\", Scalar([1, 2, 3]).data)\n",
    "print(\"Scalar.arange(0, 1, 0.25) ->\", Scalar.arange(0.0, 1.0, 0.25).data)\n",
    "ref = SR2.zeros(3, dtype=torch.float64)\n",
    "print(\"from_value inherits dtype ->\", Scalar.from_value(0.5, like=ref).dtype)"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "14f93778",
   "metadata": {},
   "source": [
    "## See also\n",
    "\n",
    "- [](tensor-types-regions) — how the leading axes split into batch\n",
    "  regions once you start batching these types.\n",
    "- [](syntax-catalog) — the per-type option reference for every\n",
    "  registered object."
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
