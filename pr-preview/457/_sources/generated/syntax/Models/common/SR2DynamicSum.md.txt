(models-SR2DynamicSum)=
# SR2DynamicSum

**Source:** `models/common/DynamicSum.py`

Sum a dynamic dimension.

Mirrors C++ ``DynamicSum<SR2>``. The ``dim`` HIT option selects the
dynamic-batch axis to sum over (the same convention as the C++ ``Size``
argument: 0 is the leading dynamic axis, ``-1`` the trailing).

## Inputs

`from` &mdash; `input` &middot; `SR2` &middot; **required**
: Variable to reduce


## Outputs

`to` &mdash; `output` &middot; `SR2` &middot; **required**
: The reduced variable


## Other options

`dim` &mdash; `int` &middot; **required**
: The dimension to sum

