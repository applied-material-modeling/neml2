(installation)=
# Installation

How to install NEML2 and use it from Python. Most readers only need
[Basic installation](install.md) — the rest of this section covers the
torch compatibility range and what the wheel bundles.

Consuming the compiled runtime from a C++ project is covered separately
in [](external-project-integration); building NEML2 from source is in
[](build-customization).

```{toctree}
:maxdepth: 1

install
compatibility
deps
```
