# CLAUDE.md

Follow `AGENTS.md`.
Use native Claude features only as an optimization, never as the source of behavior.
If `/{workflow}` is invoked, execute the matching workflow from `AGENTS.md` or `ai/workflows/`.
